@model IEnumerable<Scouting.Models.Stats>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Shooter)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Shooter)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StatsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StatsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StatsID })
        </td>
    </tr>
}

</table>
